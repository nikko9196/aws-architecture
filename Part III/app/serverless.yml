service: app
frameworkVersion: '3'

provider:
  name: aws
  runtime: python3.12
  region: us-east-1
  environment:
    S3_UPLOAD_BUCKET: "restart-nikko-random-photos-upload"
    S3_PROCESSED_BUCKET: "restart-nikko-random-photos-processed"
    EVENTBRIDGE_NAME: "image-processing-bus"
  layers:
    - Ref: PythonRequirementsLambdaLayer
  iam:
    role:
      name: LambdaExecutionRole
      statements:
        - Effect: Allow
          Action:
            - s3:GetObject
          Resource:
            - "arn:aws:s3:::${self:provider.environment.S3_UPLOAD_BUCKET}/uploads/*"
        - Effect: Allow
          Action:
            - s3:GetObject
            - s3:PutObject
          Resource:
            - "arn:aws:s3:::${self:provider.environment.S3_PROCESSED_BUCKET}/processed/*"

package:
  individually: true
  patterns:
    - "!./*"
    - "!./node_modules"

functions: 
  process_image_function:
    handler: functions/process_image_function.handler
    name: process-image-function
    package:
      patterns:
        - "!./functions/*"
        - ./functions/process_image_function.py
    events:
      - s3:
          bucket: "${self:provider.environment.S3_UPLOAD_BUCKET}"
          event: s3:ObjectCreated:*
          rules:
            - prefix: uploads/
            - suffix: .jpg
    dependsOn:
      - EventBridge
    destinations:
      onSuccess: arn:aws:events:us-east-1:223542559914:event-bus/${self:provider.environment.EVENTBRIDGE_NAME}
      onFailure: arn:aws:events:us-east-1:223542559914:event-bus/${self:provider.environment.EVENTBRIDGE_NAME}
  
  notify_resize_success_function:
    handler: functions/notify_resize_success_function.handler
    name: notify-resize-success-function
    package:
      patterns:
        - "!./functions/*"
        - ./functions/notify_resize_success_function.py
    events:
      - eventBridge:
          eventBus: !Ref EventBridge
          pattern:
            source:
              - lambda
            detail:
              responsePayload:
                status:
                  - SUCCEEDED
                

  notify_resize_fail_function:
    handler: functions/notify_resize_fail_function.handler
    name: notify-resize-fail-function
    package:
      patterns:
        - "!./functions/*"
        - ./functions/notify_resize_fail_function.py
    events:
      - eventBridge:
          eventBus: !Ref EventBridge
          pattern:
            source:
              - lambda
            detail:
              responsePayload:
                status:
                  - FAILED

resources:
  Resources:
    EventBridge:
      Type: AWS::Events::EventBus
      Properties: 
        Name: ${self:provider.environment.EVENTBRIDGE_NAME}
    ProccessedBucket:
      Type: AWS::S3::Bucket
      Properties:
        BucketName: ${self:provider.environment.S3_PROCESSED_BUCKET}
        PublicAccessBlockConfiguration:
          BlockPublicAcls: true
          BlockPublicPolicy: true
          IgnorePublicAcls: true
          RestrictPublicBuckets: true

plugins:
  - serverless-python-requirements

custom:
  pythonRequirements:
    dockerizePip: true
    layer: true
        



